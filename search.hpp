/**
   Binary Search
   - Time: O(logn), Space: O(1)
 */
int binarysearch(int array[],int size, int key);

/**
   Binary Search (Recursive)
   - Time: O(logn), Space: O(logn)
 */
int rbinarysearch(int array[],int l, int h, int key);
